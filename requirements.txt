# Full application: Django web app + ML pipeline.
# Pinned to the versions the reported results were produced with.
# For the ML pipeline alone, see ml/requirements.txt.

Django==6.0.6

pandas==3.0.4
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.3.0
shap==0.52.0
matplotlib==3.11.0
pyarrow==24.0.0
joblib==1.5.3
